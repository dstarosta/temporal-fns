# temporal-fns

A [`date-fns`](https://github.com/date-fns/date-fns) compatible API built on the native [`Temporal`](https://tc39.es/proposal-temporal/docs/) object.

## Requirements

| Toolchain    | Version  |
| ------------ | -------- |
| `typescript` | >= 5.9.0 |

Runtime: requires `Temporal` to be globally available (Chrome/Edge/Firefox evergreen, Node >= 26).

## Installation

```sh
npm install temporal-fns
```
